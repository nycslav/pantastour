# External integrations

Provider adapters belong here: maps, PAGASA, Firebase Cloud Messaging, photo storage, RevenueCat, and OpenAI. Feature modules depend on interfaces rather than provider implementations.

